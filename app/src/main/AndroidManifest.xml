<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.petitesannonces">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PetitesAnnonces">
        <activity android:name=".Liste_Mes_Annonces"></activity>
        <activity android:name=".ListeChat" />
        <activity android:name=".Chat" />
        <activity android:name=".Annonce" />
        <activity android:name=".Liste_Annonces" />
        <activity android:name=".AjoutAnnonce" />
        <activity android:name=".Menu" />
        <activity android:name=".Inscription" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>